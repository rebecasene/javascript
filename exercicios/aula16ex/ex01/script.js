function adicionar() {
    let num = document.getElementById('n')
    let selec = document.getElementById('s')

    if (num < 1 || num > 100) {
        alert('Velor inválido ou já encontrado na lista')

    } else {
        let numero = Number(num.value)

    }
    
}